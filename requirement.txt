# Install from the project root with:
# python3 -m pip install -r requirement.txt
#
# LiveComment uses only the Python standard library at runtime.
# Editable installation also registers the `livecomment` command.
-e .
